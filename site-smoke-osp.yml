- hosts: workstation
  gather_facts: false
  tasks:
  - name: OpenStack servers
    os_server_info:
      cloud: openstack
    register: result
  - debug:
      var: item.name
    loop: "{{ result.openstack_servers }}"
    when: item.name == 'frontend'

#  - name: Curl website
#    uri:
#      url: "http://{{result.openstack_servers.public_v4}}/
#    register: webpage
#    when: {{ result.openstack_servers.metadata.group }} == frontends
#
#  - name: Fail if 'Ansible has done its job' is not in the page content
#    fail:
#    when: "'Ansible has done its job' not in webpage.results[0].content"
#    tags:
#      - osp.smoke
